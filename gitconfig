[alias]
        co = checkout
        br = branch
        ci = commit
        st = status
        unstage = reset HEAD --
        last = log -1 HEAD
[color]
        ui = auto
[merge]
        tool = opendiff
[push]
        default = simple
[core]
        editor = emacsclient -t
        excludesfile = ~/.gitignore_global
